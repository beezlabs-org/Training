beekeeper port : 7229
jhive port : 7433
keyserver : 7336
queue port : 7339
workflow port : 7341

flow of script:
input: path of dir
name of .jar file ---> just port number is enough to grep
steps:
1) navigate to the folder of .jar file
2) if process is already running kill the process
and run it
3) else start the process
4) do this for all the directories in the tulip directory

flow for options:
steps:
1) providing options to get the server(navigate
to dir) to be launched
2) providing an option with (-f to force-start the server
and without -f the server will be soft-launched)

//when f is NOT an option i would like to
soft start the server